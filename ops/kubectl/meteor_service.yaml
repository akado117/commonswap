apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: commonswap-prod
    tier: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    app: guestbook
    tier: frontend
